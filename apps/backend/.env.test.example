# Скопируйте в `.env.test` (файл в .gitignore):
#   cp .env.test.example .env.test
#
# Отдельная БД для Vitest — не трогает dev `carsharing`.
# Postgres: `pnpm db:up` → `pnpm db:create:test` → `pnpm test:setup`

DATABASE_URL="postgresql://carsharing:carsharing@localhost:5432/carsharing_test?schema=public"

AUTH_TOKEN_SECRET="test-secret-change-me"
