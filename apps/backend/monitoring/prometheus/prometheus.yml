global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: carsharing-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          # host.docker.internal: в compose для prometheus задан extra_hosts (host-gateway).
          - host.docker.internal:3000
